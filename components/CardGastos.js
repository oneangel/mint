import React from 'react';
import { Card } from 'react-native-paper';

const TotalGastos = () => {
  
  return (
    <Card mode='contained' style={{alignItemswidth: 236, height: 90, marginRight:10, borderRadius: 20, backgroundColor: '#FFECEC'}}>
      <Card.Content style={{justifyContent:'flex-end'}}>

      </Card.Content>
    </Card>
  );
};

export default TotalGastos;
